//Stored procedure to apply a discount on a given product

DELIMITER //
CREATE PROCEDURE apply_discount
(IN product_id CHAR(10))
BEGIN
    INSERT INTO discount VALUES("1001", product_id, CURDATE(), CURDATE() + 7);
END //
DELIMITER ;

